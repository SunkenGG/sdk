package gg.sunken.sdk.io;

/**
 * An enum for the different HTTP methods (verbs).
 * @author santio
 */
@SuppressWarnings("MissingJavadoc")
public enum MethodType {
    
    GET,
    POST,
    PUT,
    DELETE,
    PATCH,
    HEAD,
    OPTIONS,
    TRACE,
    CONNECT,
    ;
    
}
